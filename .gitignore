# Python bytecode / caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Virtual environment
.venv/

# OS / editor cruft
.DS_Store
*.swp

# Kept local-only for now (not published to the repo yet)
BUILD_PROCESS.md
README.md
DECISIONS.md

# Datasets — fetched from the public ETH mirror (see README); never committed to git
data/

# Personal study notes — local-only scratchpad for understanding the project
notes/

# Chunk 5 — W&B local run cache and saved model checkpoints
wandb/
checkpoints/
